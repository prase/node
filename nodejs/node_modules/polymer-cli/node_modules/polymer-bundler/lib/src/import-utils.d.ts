import { ASTNode } from 'parse5';
import { UrlString } from './url-utils';
export declare function inlineScript(docUrl: UrlString, externalScript: ASTNode, loader: (url: UrlString) => Promise<string>): Promise<string | undefined>;
export declare function inlineStylesheet(basePath: string | undefined, docUrl: UrlString, cssLink: ASTNode, loader: (url: UrlString) => Promise<string>): Promise<ASTNode | undefined>;
/**
 * Given a string of CSS, return a version where all occurrences of urls,
 * have been rewritten based on the relationship of the import url to the
 * main doc url.
 * TODO(usergenic): This is a static method that should probably be moved to
 * urlUtils or similar.
 */
export declare function rewriteImportedStyleTextUrls(basePath: string | undefined, importUrl: string, mainDocUrl: string, cssText: string): string;

import { Analyzer } from 'polymer-analyzer';
export interface DepsIndex {
    entrypointToDeps: Map<string, Set<string>>;
}
export declare function buildDepsIndex(entrypoints: string[], analyzer: Analyzer): Promise<DepsIndex>;

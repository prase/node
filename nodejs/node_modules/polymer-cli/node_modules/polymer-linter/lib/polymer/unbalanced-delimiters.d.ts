import { ParsedHtmlDocument } from 'polymer-analyzer/lib/html/html-document';
import { Warning } from 'polymer-analyzer';
import { HtmlRule } from '../html/rule';
/**
 * Unbalanced binding expression delimiters occurs when a value such as
 * `[[myValue]]` or `{{myValue}}` have too many or too few brackets on either
 * side.
 */
export declare class UnbalancedDelimiters extends HtmlRule {
    code: string;
    description: string;
    constructor();
    checkDocument(parsedHtml: ParsedHtmlDocument): Promise<Warning[]>;
    private _getWarningsForElementAttrs(parsedHtml, element);
    private _getWarningsForTemplate(parsedHtml, template);
    private _getMessageForBadBindingExpression(text);
    private _extractBadBindingExpression(text);
}
